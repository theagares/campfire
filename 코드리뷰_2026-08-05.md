# Campfire 코드 리뷰 — 2026-08-05

엔진(FastAPI) · 익스텐션(MV3) · 데스크탑(Electron) 세 컴포넌트를 **신뢰 경계** 중심으로 읽은 결과. 총 16건.

인용한 줄 번호는 리뷰 시점(`a48b359`) 기준이며 전부 직접 확인했다. 라벨 불일치는 grep으로 교차 검증했다.

> **정정 (같은 날, 수정 작업 중).** 최초 리뷰에서 1번을 "임의 웹사이트가 `/mcp`의 `secure_read_file`로 로컬 파일을 읽을 수 있다"로 적었다. **이 부분은 틀렸다.** 실행 중인 설치본에 직접 붙어 확인한 결과, `/mcp`는 MCP SDK(1.29.0)의 DNS rebinding 보호가 Origin을 검사해 이미 403(`Invalid Origin header`)으로 막고 있었다. 첫 리뷰 시점에 엔진이 떠 있지 않아 코드만 읽고 판단했고, SDK가 자체 방어를 갖고 있다는 걸 확인하지 않은 채 결론을 냈다. 실제 노출 범위는 아래 1번에 다시 정리했다 — 여전히 실재하는 문제지만 심각도는 "심각"이 아니라 "중간"이다.

**우선순위 1·2·3·4는 이 리뷰 직후 수정했다** — 아래 각 항목의 "수정됨" 표시와 문서 끝 [수정 내역](#수정-내역-2026-08-05) 참고.

---

## 요약

| # | 심각도 | 항목 | 상태 |
|---|--------|------|------|
| 1 | ~~심각~~ **중간** | 엔진 무인증 + CORS 전면 개방 → 임의 웹사이트가 REST를 호출·판독 (파일 읽기는 **아님**, 위 정정 참고) | ✅ 수정됨 |
| 2 | 심각 | `.env`가 `secure_read_file` 텍스트 화이트리스트에 포함 | ✅ 수정됨 |
| 3 | 심각 | `INJECTION_POLICY=block`이 MCP 경로에서 미강제 | ✅ 수정됨 |
| 4 | 중간 | `_infer()` 무타임아웃 → 엔진 영구 정지 가능 | ✅ 수정됨 |
| 5 | 중간 | idle watcher와 `detect()` 사이 레이스 | ✅ 수정됨 |
| 6 | 중간 | `httpx.AsyncClient` 락 없는 lazy 생성 + 미해제 | ✅ 수정됨 |
| 7 | 중간 | `secure_search_files`가 매칭 라인마다 전체 파이프라인 실행 | ✅ 수정됨 |
| 8 | 중간 | `mask_text`가 외부 입력을 검증하지 않음 | ✅ 수정됨 |
| 9 | 중간 | 마스킹 라벨 사전이 3벌이고 이미 어긋남 | ✅ 수정됨 |
| 10 | 중간 | 사이드패널이 겹치는 탐지를 병합하지 않음 | ✅ 수정됨 |
| 11 | 낮음 | `bridgeToken` 주석이 실제보다 강한 보장을 약속 | ✅ 수정됨 |
| 12 | 낮음 | `_split_chunks` docstring이 config와 반대말 | ✅ 수정됨 |
| 13 | 낮음 | `jobs.py` `_public()`이 항등함수 | ✅ 수정됨 |
| 14 | 낮음 | `_safe_extract`가 링크 멤버를 못 잡음 | ✅ 수정됨 |
| 15 | 낮음 | 다운로드 진행 이벤트가 1MB마다 쌓임 | ✅ 수정됨 |
| 16 | 낮음 | `/health` 확장 감지가 확장 ID 미검증 | ✅ 주석 정정 |
| **17** | **중간** | **`.txt`가 `secure_search_files`에서 통째로 빠져 있었다** (7번 수정 중 발견) | ✅ 수정됨 |

---

## 심각 — 즉시 수정 대상

### 1. 엔진이 무인증인데 CORS가 전면 개방 → 아무 웹사이트나 REST를 호출하고 응답을 읽는다 ✅

`engine/app/main.py:51-56`이 `allow_origins=["*"]`이고, 엔진 어디에도 인증 토큰·Origin 검사가 없다(grep으로 확인 — origin을 보는 곳은 `health.py:32`의 "확장 봤음" 표시용 한 줄뿐).

**실측 확인** (실행 중인 설치본, 2026-08-05):

```
$ curl -H "Origin: https://evil.example" -D - http://127.0.0.1:48200/health
HTTP/1.1 200 OK
access-control-allow-origin: *          ← 아무 웹 출처에나 응답 판독을 허용
```

포트 후보도 48200~48209 열 개뿐이라 탐색이 즉시 끝난다. 사용자가 아무 페이지나 열어둔 상태에서 그 페이지의 JS가 할 수 있는 것:

- **설치 여부·상태 지문 채취** — `/health`가 엔진 상태, 바인딩 포트, 모델 준비 여부, 지원 확장자, 정책을 그대로 준다.
- **로컬 ML 파이프라인 무단 사용** — `/jobs`, `/jobs/prompt`를 임의로 호출. 인젝션이 걸리면 2단계 위치 특정이 Upstage를 호출하므로 **사용자 API 키로 과금**까지 유발된다(`llm_mcp.py:410`).
- **수백 MB 다운로드 유발** — `/models/fetch`.

#### 틀렸던 부분 (정정)

최초 리뷰에서는 여기에 "`/mcp`의 `secure_read_file`로 홈 디렉터리를 읽어갈 수 있다"를 덧붙였다. **그건 사실이 아니다.**

```
$ curl -X POST -H "Origin: https://evil.example" ... http://127.0.0.1:48200/mcp/
HTTP/1.1 403 Forbidden
Invalid Origin header                   ← MCP SDK 자체 DNS rebinding 보호
```

MCP Python SDK(설치본 1.29.0)의 `TransportSecurityMiddleware`가 Origin을 검사해 이미 막고 있었다. 리뷰 당시 엔진이 떠 있지 않아 코드만 읽고 결론을 냈고, SDK가 자체 방어를 갖고 있는지 확인하지 않은 게 원인이다.

다만 이 방어는 **우리 코드가 아니라 SDK 기본값**이고, `pyproject.toml`의 허용 범위가 `mcp>=1.2,<2.0`으로 넓어 어떤 1.x가 해석되느냐에 딸려 있다(SDK 안에서도 `TransportSecurityMiddleware`를 인자 없이 생성하면 보호가 꺼진 채 시작한다). 파일 접근이 걸린 경로의 안전을 의존성 해석 결과에 맡길 이유는 없으므로, 명시적 가드는 그대로 넣었다.

> 관련해서 남는 진짜 문제: `tools.py:51`의 `_resolve()`는 절대경로를 `resolve()`만 할 뿐 `_PROJECT_ROOT` 밖을 막지 않는다. 브라우저에서는 못 닿지만, **연결된 AI 클라이언트는 임의 경로를 읽을 수 있다** — 문서에 심긴 프롬프트 인젝션이 AI에게 `secure_read_file("~/.ssh/id_rsa")`를 시키는 시나리오가 정확히 이 게이트웨이가 막으려는 것이다. 2번 수정이 자격증명 파일을 차단하면서 최악의 경로는 닫혔지만, 루트 제한 자체는 후속 과제로 남는다.

**처방**

- `allow_origins`를 확장 출처(`chrome-extension://<id>`)로 한정. 웹페이지가 단순 요청을 *보내는* 것은 CORS로 막을 수 없지만, 응답을 못 읽으면 결과를 가져가지 못한다.
- `/mcp`는 **Origin 헤더가 붙은 요청을 거부**(SDK와 중복이지만 우리 쪽 보장). Origin은 브라우저가 스스로 붙이고 페이지 스크립트가 지울 수 없으므로 우회되지 않는다.
- (후속 과제) 로컬 토큰: 엔진이 기동 시 랜덤 토큰을 userData에 쓰고 확장이 `Authorization`으로 붙인다. 웹페이지는 그 파일을 못 읽으니 막힌다. `service-worker.js:91-92`에 이미 자리가 잡혀 있다. **요청을 보내는 것 자체를 막으려면 이게 필요하다** — CORS만으로는 판독만 막힌다.
- (후속 과제) `_resolve()`에 루트 제한.

### 2. `.env`가 `secure_read_file`의 텍스트 화이트리스트에 있다 ✅

`tools.py:43`의 `_TEXT_EXTS`에 `".env", ".example"`이 들어 있고, `_file_kind()`는 `.env*`를 명시적으로 text로 승격한다(`tools.py:62`). 마스커는 PII와 인젝션만 지우므로 `UPSTAGE_API_KEY=...`, AWS 키, DB 비밀번호는 그대로 통과한다.

"보안 게이트"를 표방하는 도구가 바이너리는 막으면서(`tools.py:299-306`) 자격증명 파일은 여는 구조라 방향이 반대다. 이런 파일은 *마스킹해서 주는* 대상이 아니라 **애초에 게이트를 통과시키지 않는** 대상이다 — 무엇이 비밀인지 내용만 보고 알아낼 방법이 없기 때문이다.

### 3. `INJECTION_POLICY=block`이 MCP 경로에서 전혀 강제되지 않는다 ✅

`orchestrator.py:184`가 `blocked`를 계산하지만 `masked_text`는 그대로 반환하고, `tools.py:316-326`의 `secure_read_file`은 **`blocked` 필드를 응답에서 아예 빼고** `content`를 넘겨준다. `decision`도 `masked|clean` 둘뿐이다 — docstring(`tools.py:290`)은 "decision: masked | clean | blocked"라고 적혀 있는데 실제로 blocked가 나오는 건 바이너리 경로뿐이다.

즉 정책을 block으로 켜도 AI 클라이언트는 내용을 그대로 받는다. **정책이 문서상으로만 존재한다.** `secure_read_file`은 PLAN §4.2 게이트라, 여기서 막지 않으면 막을 곳이 없다.

---

## 중간 — 안정성

### 4. `_infer()`에 타임아웃이 없어 엔진이 영구히 멎을 수 있다 ✅

`llm_mcp.py:252-270`. `_read_ready_line`은 타임아웃이 있는데(`:159`), 정작 추론 응답 대기는 `proc.stdout.readline()`을 **무한 대기하면서 `_request_lock`을 쥔 채로** 한다. 서브프로세스가 살아는 있으나 응답을 못 주는 상태가 되면 이후 모든 인젝션 검사가 영구 블록되고, 확장 쪽 `pollJobEvents`(`service-worker.js:146`)도 종료 조건이 done/error뿐이라 800ms마다 영원히 폴링한다.

타임아웃을 넣을 때 **`request["id"]` 대조를 같이 넣어야 한다** — 지금은 락 덕분에 1:1이라 id를 안 봐도 되지만(`:245`에서 만들기만 하고 검증 안 함), 타임아웃이 생기면 늦게 도착한 이전 응답을 다음 요청 결과로 오인한다.

### 5. idle watcher와 `detect()` 사이 레이스

`_idle_watcher`는 `_proc_lock`만 잡고 `_kill_process()`를 한다(`llm_mcp.py:237-238`). `_ensure_process()` 통과 후 `_infer()` 진입 전에 이게 끼면 `_infer`의 `assert proc is not None`(`:254`)이 터져 500이 난다. `python -O`로 돌면 assert가 사라져 `None.stdin` AttributeError가 되고. 창은 좁지만 10분 유휴 직후 첫 요청에서 밟을 수 있다.

> 4번 수정에서 `assert`를 명시적 `RuntimeError`로 바꿔 크래시 형태는 개선했다. 레이스 자체(`_request_lock`을 watcher가 안 잡는 것)는 남아 있다.

### 6. `httpx.AsyncClient`를 락 없이 lazy 생성 + close 안 함

`llm_mcp.py:437-438`. `_detect_all`이 청크를 `asyncio.gather`로 동시에 돌리므로(`orchestrator.py:82`) 두 청크가 동시에 미스나면 클라이언트가 둘 만들어지고 하나는 누수된다. 어차피 닫는 곳도 없어 프로세스 종료까지 유지된다.

### 7. `secure_search_files`가 매칭 라인마다 전체 파이프라인을 돈다

`tools.py:378`이 라인 하나당 `run_pipeline`을 부른다 — 라인마다 PII 인코더 + EXAONE 추론이고, misaligned가 나오면 **Solar 유료 호출까지** 붙는다. `max_results=50`이면 최악 50회 직렬 추론 + 50회 API 과금이다.

여기서 필요한 건 탐지가 아니라 스니펫 마스킹이므로, 파일 단위로 한 번 검사한 뒤 그 결과로 라인을 자르거나 최소한 masker만 적용하는 편이 맞다.

### 8. `mask_text` MCP 도구가 외부 입력을 검증하지 않는다

`masker.merge_overlapping`은 `x["start"]`, `cur["type"]`(`masker.py:115,124`), `placeholder_for(item["type"])`(`:148`)를 그대로 인덱싱한다. `type`이 빠졌거나 `start`가 문자열이면 KeyError/TypeError로 500이다. `mask_text`(`tools.py:268`)는 AI가 채워 보내는 입력이라 방어가 필요하다.

`db.record_job`(`db.py:52,66`)도 같은 패턴인데 `tools.py:_record`만 try/except로 감쌌고 `jobs.py:61,106`은 안 감싸서, **통계 기록 실패가 정상 검사 결과를 500으로 바꾼다.**

---

## 일관성 — 같은 로직이 여러 벌이고 이미 어긋났다

### 9. 마스킹 라벨 사전 드리프트

```
engine/app/core/masker/masker.py:27      ORGANIZATION → "기관명"
extension/utils/masker.js:15             ORGANIZATION → "기관명"
extension/sidepanel/sidepanel.js:17      ORGANIZATION → "조직기밀"   ← 어긋남
```

이게 표시 문제로 안 끝나는 이유는, 사이드패널이 최종 전송 텍스트를 `buildFinalTextFrom()`으로 **직접 만들기** 때문이다(`sidepanel.js:479-484`). ORGANIZATION이 잡히면 실제로 사이트에 `[조직기밀 마스킹]`이 나가고, 엔진이 만든 `maskedText`·저장된 통계와 다른 문자열이 된다.

근본 처방은 라벨·병합 규칙을 한 곳(엔진)에 두고 익스텐션은 `applied`를 받아 쓰는 것. 범위가 크면 최소한 `TYPE_LABELS`만이라도 한 파일에서 import하도록 통일.

### 10. 사이드패널이 겹치는 탐지를 병합하지 않는다

엔진 `merge_overlapping`(`masker.py:107`)은 겹침을 하나로 합치는데, `buildSegments`(`sidepanel.js:78-96`)는 `start = Math.max(it.start, cursor)`로 잘라 둘 다 남긴다. PII와 인젝션이 겹치면 엔진은 `[인젝션 마스킹]` 하나, 패널은 `[이름 마스킹][인젝션 마스킹]` 둘을 만든다. 같은 입력에 결과가 둘인 셈.

---

## 낮음

### 11. `bridgeToken`은 위조 방어가 아니다

`content.js:36-43`이 토큰을 `window.postMessage(..., '*')`로 MAIN world에 뿌리는데, MAIN world는 페이지 자신의 컨텍스트다. 페이지가 `message` 리스너 하나만 달면 토큰을 읽는다. `content.js:804`의 `// 위조 메시지 차단` 주석이 실제보다 강한 보장을 약속한다.

이 채널로 오는 게 "검사 요청"까지라 실피해는 제한적이니, 코드를 바꾸기보다 주석을 사실대로 낮추는 쪽을 권한다(MAIN world 브리지에서는 원리적으로 못 막는다).

### 12. `_split_chunks` docstring이 config와 반대말을 한다

`orchestrator.py:36`은 "1,500자 청크"라고 하는데 실제 기본값은 `CHUNK_SIZE=1000`이고, `config.py:59-61`에는 1,000이 더 낫다는 실측 근거까지 적혀 있다.

### 13. `jobs.py:110`의 `_public()`은 항등함수

`tools.py`의 동명 함수(진짜로 원문을 걷어냄)와 이름이 같아 "HTTP 쪽도 걸러진다"고 오해하기 쉽다. 없애고 주석만 남기는 게 낫다.

### 14. `_safe_extract` 대신 `extractall(..., filter='data')`

`models.py:105`의 손으로 쓴 경로 검사는 심볼릭 링크 멤버를 안 본다. tarball이 체크섬 고정이라 실위험은 사실상 0이지만, 표준 필터가 더 넓게 막고 더 짧다(Python 3.12+).

### 15. 다운로드 진행 이벤트가 job_registry에 전부 쌓인다

`models.py:132`가 1MB마다 emit하므로 476MB면 ~476개 + 백본까지. `get_events`(`job_registry.py:45`)가 매 폴링마다 전체를 순회한다. 진행 이벤트는 마지막 것만 유지(coalesce)하면 된다.

### 16. `/health`의 확장 감지가 확장 ID를 검증하지 않는다

`health.py:32-34`는 `chrome-extension://`로 시작하기만 하면 "확장 봤음"으로 기록한다. 아무 확장이나 이 표시를 켤 수 있다. 표시용이라 영향은 미미.

우리 확장의 ID는 개발용 언패킹 로드에서 매번 달라져 고정 검증이 불가능하다 — 코드를 바꾸는 대신 "성긴 신호이며 접근 제어 근거로 쓰면 안 된다"를 주석에 못 박았다.

---

## 추가 발견 (7번 수정 중)

### 17. `.txt`가 `secure_search_files`에서 통째로 빠져 있었다 ✅

`_file_kind()`는 문서 판정을 먼저 하고(`suffix in _DOCUMENT_EXTS`), `.txt`는 `config.SUPPORTED_EXTENSIONS`에 들어 있다 — 그래서 `.txt`의 kind는 `"document"`다. 그런데 `secure_search_files`는 `kind != "text"`인 파일을 전부 건너뛴다.

**결과: 도구 설명은 "텍스트 파일을 검색한다"인데 `.txt`는 한 건도 안 나왔다.** `.md`·`.py`·`.json`처럼 `_TEXT_EXTS`에만 있고 `_DOCUMENT_EXTS`에는 없는 확장자만 검색됐다.

리뷰 때 못 잡은 이유가 분명하다 — 코드를 읽을 때 `_TEXT_EXTS`에 `.txt`가 없는 걸 보고도 "`.txt`는 당연히 텍스트겠지"라고 넘겼고, `_file_kind`의 판정 **순서**까지 따라가지 않았다. 7번 회귀 테스트를 `.txt` 픽스처로 쓰다가 매칭이 0건으로 나와서 드러났다.

부수적으로, 이 버그가 `test_activity.py::test_mcp_search_does_not_flood_activity`를 **공허하게** 만들고 있었다 — 픽스처가 `hits.txt`라서 루프가 한 번도 실행되지 않았고, 그래서 "방송하지 않는다"는 단언이 항상 통과했다. `.txt`를 고치자 그 테스트가 처음으로 실제 코드를 태우면서 `KeyError: 'blocked'`를 냈다(스텁 결과에 그 키가 없었다 → `.get()`으로 방어).

분류(`_file_kind`) 자체를 바꾸면 `secure_read_file`의 mime 추정 등 다른 판단까지 흔들리므로, "검색 가능한 평문"이라는 별도 기준(`_SEARCHABLE_TEXT_EXTS`)을 두는 쪽으로 고쳤다.

---

## 잘 되어 있는 부분

의견을 붙이는 게 공평할 것 같아 적는다.

- `_redact_items`/`_public`(`tools.py:67-105`)에서 **MCP 소비자가 AI라는 이유로 원문을 걷어내는** 판단. 같은 파이프라인 결과를 소비자에 따라 다르게 노출하는 게 정확하다.
- Solar 응답을 `None`/`[]`/`[spans]` 세 갈래로 나눠 **"판단 불가"와 "없다고 답함"을 구분**한 설계(`llm_mcp.py:410-484`). 이 구분이 없으면 오탐 정정이 진짜 인젝션을 흘려보내는 경로가 된다.
- 모델 가중치를 설치 폴더 밖에 두는 결정(`config.py:14-22`).

셋 다 실사용자 리포트에서 역산된 근거가 주석에 남아 있어 왜 그런지 읽는 사람이 알 수 있다. 주석 밀도가 높은 편인데, 이 코드베이스에서는 그게 부담이 아니라 자산으로 작동한다.

---

## 수정 내역 (2026-08-05)

브랜치 `fix/engine-local-attack-surface`. 우선순위 1·3·2·4를 반영했다.

### 1번 — 로컬 공격면 차단

- `engine/app/main.py` — `allow_origins=["*"]` → `allow_origin_regex`로 확장 출처(`chrome-extension://` 등)만 허용. 웹페이지는 응답을 읽지 못한다.
- `engine/app/adapters/mcp/__init__.py` — `_mcp_asgi`가 **Origin 헤더가 붙은 요청을 403으로 거절**. 확장 출처도 예외 없이 거절한다(확장은 REST만 쓰고 `/mcp`는 안 쓴다 — 확장이 침해돼도 파일 접근 도구는 안 열린다). 현재 SDK 버전에서는 중복 방어다(위 정정 참고).

`/mcp`는 Starlette Mount라 슬래시 없는 `/mcp`는 `/mcp/`로 307 리다이렉트되고 가드는 그 뒤에서 걸린다. 307은 메서드·본문을 보존하므로 우회로는 아니지만, 테스트는 두 경로를 모두 고정한다.

### 2번 — 자격증명 파일 반환 금지

- `engine/app/adapters/mcp/tools.py` — `_TEXT_EXTS`에서 `.env`/`.example` 제거. `_is_secret_file()` + `"secret"` 종류 신설(`.env*`, `.pem`, `.key`, `id_rsa`, `.netrc`, `.npmrc`, `.p12` 등).
- `secure_read_file`은 `decision="blocked"` + `content=""`, `scan_file`은 `PermissionError`(마스킹본을 통한 우회로 차단), `scan_files`는 `skipped`로 분류.

### 3번 — block 정책 강제

- `_public()` — blocked면 `recommendedAction="block"`. 이 응답을 읽는 게 사람이 아니라 AI라 권고 문구가 실질적 지시로 작동한다.
- `secure_read_file` — blocked면 `content=""` + `blocked=True`. 무엇이 걸렸는지(`stats`/`injectionItems`)는 계속 알려준다 — **내용만 막는 것이지 결과를 숨기는 게 아니다.**
- `scan_files`는 blocked 항목의 `maskedText`를 비우고, `secure_search_files`는 스니펫을 `[차단됨 — 인젝션 정책]`으로 대체(위치는 알려주되 내용은 안 준다).

### 4번 — 추론 타임아웃

- `engine/app/config.py` — `INJECTION_INFER_TIMEOUT_SEC` 신설(기본 120초, `SECUREDOC_INJECTION_INFER_TIMEOUT_SEC`).
- `llm_mcp._infer()` — `asyncio.wait_for`로 감싸고 초과 시 서브프로세스를 죽인 뒤 실패. 다음 요청의 `_ensure_process`가 새로 띄운다.
- 같은 함수에서 **응답 `id` 대조** 추가(타임아웃 도입으로 생기는 "지각 응답을 다음 요청 결과로 오인" 방지)와, `assert` → 명시적 `RuntimeError`(`python -O`에서 사라지지 않게).
- `_warmup()` — 타임아웃으로 프로세스가 죽은 채 `loaded`로 표시되지 않도록 재기동.

### 회귀 테스트

`engine/tests/test_local_attack_surface.py` 신설 — 위 4건을 각각 고정한다. 특히:

- 웹 출처에 `Access-Control-Allow-Origin`이 **안 붙는지**(200이 나는 것 자체는 CORS로 막을 수 없다)
- 확장 출처는 **계속 붙는지**(조이다가 우리 클라이언트를 끊으면 안 된다)
- 기본 정책(mask)에서는 예전대로 내용을 준다는 과잉 차단 회귀 방지

---

## 2차 수정 내역 (5~17번)

### 5번 — idle watcher 레이스

`_idle_watcher`가 `_proc_lock`만 잡고 프로세스를 죽여서, 추론이 진행 중인 파이프 밑을 빼갈 수 있었다. `_proc_lock` + `_request_lock`을 함께 잡도록 바꿨다(락 순서를 proc → request로 고정 — 반대로 잡는 경로가 없어 교착이 없다).

그래도 `_ensure_process()`와 `_infer()` 사이의 틈은 남으므로, `ProcessGone` 예외를 따로 두고 `detect()`가 **그 경우에만** 한 번 재기동 후 재시도한다. 타임아웃(응답 없음)은 이 분기로 오지 않아 120초를 두 번 기다리는 일이 없다.

### 6번 — Solar HTTP 클라이언트

`_http_lock`으로 lazy 생성을 감쌌다(동시 청크 두 개가 각각 만들어 하나가 누수되던 것). 더불어 `aclose()`를 추가하고 `registry.aclose_detectors()`를 lifespan 종료에 걸어, 커넥션 풀·유휴 워처·추론 서브프로세스를 루프가 살아있는 동안 정리한다.

### 7번 — `secure_search_files`

매칭 라인마다 `run_pipeline`을 부르던 것을 **매칭이 있는 파일당 1회**로 바꿨다. 파일 전체를 한 번 검사해 좌표를 받고, 각 라인 구간에 걸친 항목만 라인 기준으로 옮겨 마스킹한다. 결과는 같고(오히려 라인 경계에 걸친 항목까지 잡는다) 추론·과금이 라인 수에 비례하지 않는다. 매칭이 하나도 없는 파일은 검사를 아예 돌리지 않는다.

### 8번 — 외부 입력 방어

`masker.validate_and_fix`가 dict이 아니거나 `type`이 문자열이 아닌 항목을 버린다. `merge_overlapping`의 confidence 비교는 `None`을 0으로 받는다(MCP `_redact_items`를 거친 항목이 그렇게 온다). `mask_text`는 버린 개수를 `skippedCount`로 알려준다.

`jobs.py`는 `db.record_job`을 `_record()`로 감쌌다 — 검사가 성공하고 마스킹 결과까지 만든 뒤에 통계 기록 실패로 응답이 500으로 뒤집히던 것을 막는다.

### 9번 — 라벨 드리프트

**`extension/utils/masker.js`는 아무도 import하지 않는 죽은 코드였다** — 그래서 드리프트가 드러나지 않았다. 삭제했다.

남은 한 벌(`sidepanel.js`)은 **화면용 라벨과 치환용 라벨을 분리**했다. 목록에는 세분화된 이름(`명령 재정의`)을 계속 보여주고, 텍스트에 박히는 이름은 `masker.py`와 같은 규칙(`placeholderLabelOf`: 인젝션 7종 → 전부 `인젝션`, PII → `TYPE_LABELS`)을 쓴다. `ORGANIZATION`이 `조직기밀`로 나가던 것과, 인젝션 세부 유형이 `[명령 재정의 마스킹]`으로 나가던 것이 함께 고쳐진다.

### 10번 — 겹침 병합

`sidepanel.js`에 `mergeOverlapping()`을 이식했다(엔진과 같은 기준: confidence 우선, 동률이면 더 긴 구간). 부수 효과로 겹친 구간이 항목 하나가 되므로, 한쪽만 토글해 "가린 것도 아니고 안 가린 것도 아닌" 상태를 만들 수 없다.

### 11~16번

- 11: `bridgeToken` 주석을 사실대로 낮췄다(MAIN world는 페이지 컨텍스트라 토큰을 읽을 수 있고, 원리적으로 못 막는다). 이 채널로 받는 게 "검사 요청"까지이고 승인 경로는 별도라는 점을 명시.
- 12: `_split_chunks` docstring의 "1,500자"를 `config.CHUNK_SIZE`(기본 1,000) 기준으로 정정.
- 13: 항등함수 `_public()` 제거, 자리에 `_record()`.
- 14: `extractall(filter="data")`를 우선 사용하고, 없는 런타임에서는 기존 검사 + 링크 멤버 거부로 내려간다.
- 15: 진행 이벤트를 1초 간격으로 솎아낸다(마지막 것은 항상 보낸다). 476MB 자산에 ~476개가 쌓이던 것을 수십 개로.
- 16: 주석에 "성긴 신호이며 접근 제어 근거로 쓰면 안 된다"를 명시.

### 회귀 테스트 (2차)

- `engine/tests/test_review_followups.py` (24건) — 8번의 잘못된 입력 7종, 14번 링크 멤버 거부 + 정상 tar 통과, 7번의 "파일당 1회"·"매칭 없으면 0회"·block 차단·`.env` 제외, 5번의 `ProcessGone` 재시도(그리고 타임아웃은 재시도 안 함), 6번의 동시 생성 1개.
  - **4번은 가짜 서브프로세스로 실제 JSONL 프로토콜을 태워 검증한다** — 지각 응답(다른 id) 무시, 진단 잡음 라인 건너뛰기, 무응답 시 무한 대기 대신 실패, 정상 경로 유지. 실모델(EXAONE)을 띄우지 않으므로 메모리와 무관하게 돈다.
- `extension/tests/sidepanel-masking.test.js` — 9·10번. 엔진 라벨 표와 패널의 치환 라벨을 17종 전부 대조하고, 겹침 병합·입력 불변성·마스킹 해제를 확인한다.

### 검증 범위의 한계 (솔직하게)

모델을 쓰지 않는 엔진 테스트 **98건**과 익스텐션 테스트 3개 파일은 전부 통과한다. 하지만 실모델이 필요한 테스트(`test_pipeline`, `test_api`, `test_mcp`, `test_detector_registry`의 일부)는 **이 기계에서 돌지 않았다** — EXAONE(2.4GB)을 로드하려다 `OSError: 페이징 파일이 너무 작습니다 (os error 1455)`로 죽는다. 커밋 메모리 여유가 5.4GB뿐이고 설치본 앱이 이미 모델을 물고 있어서다.

실패 지점은 전부 `transformers`의 `safe_open`(첫 가중치 로드)이라 이번 변경과 무관하지만, **"돌려서 통과하는 것을 봤다"고는 말할 수 없다.** 앱을 끄고 다음으로 재확인하는 게 좋다:

```
SECUREDOC_PII_PYTHON_EXECUTABLE=<app venv python> \
SECUREDOC_INJECTION_PYTHON_EXECUTABLE=<app venv python> \
SECUREDOC_SKIP_LEGACY_MIGRATION=1 \
pytest engine/tests -q
```
