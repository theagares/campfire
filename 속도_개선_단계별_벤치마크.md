# PII·인젝션 탐지 속도 — 베이스라인부터 개선 단계별 실측

각 개선사항이 실제로 얼마나 기여했는지를 하나의 기준으로 다시 재서 정리한 표다.
기존 `속도_RAM_최적화_정리.md` 는 개선을 진행하면서 그때그때 손으로 기록한 수치라
측정 조건(콜드스타트 포함 여부, Solar 왕복 포함 여부)이 항목마다 조금씩 달랐다.
이 문서는 **모든 단계를 같은 하네스·같은 기계·같은 문서로 다시 측정**한 결과다.

## 무엇을 쟀나

측정 대상은 **PII 탐지 시간 + 인젝션 탐지 시간, 딱 두 가지**다.

포함하지 않은 것:

- **Solar Pro 3 의 인젝션 2단계 위치 특정** — 측정에서 제외했다. `UPSTAGE_API_KEY`
  를 비워 `config.INJECTION_LOCALIZE_ENABLED=False` 로 만들어 코드 경로 자체를
  타지 않게 했다(측정에서 빼는 게 아니라 아예 실행하지 않는다).
- 모델 로딩/서브프로세스 기동(콜드스타트) — 측정 시작 전에 끝내둔다. 참고용으로
  로딩 시간은 따로 기록했다.
- 문서 파싱, 마스킹, 결과 직렬화.

각 단계마다 두 값을 따로 낸다:

- **첫 요청** — 로딩 직후 첫 1회. 워밍업 개선의 효과가 여기서만 드러난다.
- **정상 상태(warm)** — 그 뒤 7회 반복의 중앙값. 반복 간 산포는 ±3~5% 수준이었다.

## 측정 환경

| 항목 | 값 |
|---|---|
| GPU | NVIDIA GeForce RTX 5070 (12GB, sm_120) |
| torch | 2.11.0+cu128 / transformers 5.14.1 |
| 문서 | `demo_pii_카드분쟁접수서.docx`(1,394자), `demo_chatgpt_고객지원리뷰.docx`(1,839자) |
| 청크 | 1,000자 / 겹침 100자 → 문서당 2청크 |
| 반복 | 로컬 7회(중앙값), API 5회(중앙값) |

> 참고: 현재 배포되는 데스크탑 앱 번들의 파이썬은 `torch 2.13.0+cpu`(CUDA 미포함)라
> 실제 사용자 기기에서는 CPU 로 돈다. 이 표는 GPU 기준이므로 절대값은 배포본과
> 다르다 — 단계 간 **상대 비교**용으로 읽어야 한다.

## 베이스라인 두 개

- **베이스라인1 (API)** — PII 탐지도 인젝션 탐지도 전부 외부 LLM API 로 하던 구성.
  이 리포지토리에는 API 기반 PII 탐지기가 없어서(PII 는 처음부터 로컬 encoder),
  비교를 위해 `engine/scripts/bench/bench_api.py` 로 따로 재현했다. 프롬프트는 새로
  짓지 않고 팀이 그 용도로 써 뒀던 `파이프라인/server/pipeline/prompts.py` 의
  `PII_SYSTEM`/`INJECTION_SYSTEM` 을 그대로 썼다. 청크마다 Solar Pro 3 를 2회 호출
  (PII용·인젝션용), 청크끼리는 동시 실행, 캐시 없음.
- **베이스라인2 (단일 seed)** — 로컬 모델로 옮긴 직후, 이후 개선사항이 하나도 안
  들어간 상태(`155c9bd`).

---

## 정상 상태(warm) 탐지 시간 — 문서 2건 합산

| 단계 | 커밋 | PII 탐지 | 인젝션 탐지 | 합계 | 직전 대비 | 베이스라인1(API) 대비 | 베이스라인2 대비 |
|---|---|---:|---:|---:|---:|---:|---:|
| **베이스라인1** API(Solar Pro 3) | — | 10.04s | 937ms | **10.97s** | — | 1.00× | — |
| (참고) 3-seed 앙상블 | `155c9bd~1` | 630ms | 234ms | **864ms** | — | 12.71× | 0.72× |
| **베이스라인2** 단일 seed | `155c9bd` | 389ms | 235ms | **624ms** | 1.38× | 17.58× | 1.00× |
| + 워밍업 호출 | `72471d8` | 404ms | 236ms | **640ms** | 0.98× | 17.15× | 0.98× |
| + Solar 호출 캐싱 | `5a0c32b` | 388ms | 233ms | **621ms** | 1.03× | 17.67× | 1.01× |
| + gazetteer Aho-Corasick | `e0a02b7` | 146ms | 233ms | **379ms** | 1.64× | 28.98× | 1.65× |
| + 청크 동시 실행 | `72e8699` | 148ms | 234ms | **382ms** | 0.99× | 28.69× | 1.63× |
| + HF_HUB_OFFLINE | `e4ce280` | 145ms | 231ms | **376ms** | 1.02× | 29.17× | 1.66× |
| + CRF CPU decode | `786e8e3` | 107ms | 235ms | **343ms** | 1.10× | 32.03× | 1.82× |
| 현재 HEAD | `HEAD` | 111ms | 236ms | **347ms** | 0.99× | 31.66× | 1.80× |

## 첫 요청(로딩 직후 1회) 탐지 시간 — 문서 2건 합산

| 단계 | 커밋 | PII 탐지 | 인젝션 탐지 | 합계 | 베이스라인2 대비 |
|---|---|---:|---:|---:|---:|
| (참고) 3-seed 앙상블 | `155c9bd~1` | 973ms | 650ms | **1,623ms** | 0.74× |
| **베이스라인2** 단일 seed | `155c9bd` | 559ms | 647ms | **1,206ms** | 1.00× |
| + 워밍업 호출 | `72471d8` | 434ms | 254ms | **687ms** | 1.75× |
| + Solar 호출 캐싱 | `5a0c32b` | 412ms | 252ms | **664ms** | 1.82× |
| + gazetteer Aho-Corasick | `e0a02b7` | 172ms | 248ms | **420ms** | 2.87× |
| + 청크 동시 실행 | `72e8699` | 172ms | 260ms | **432ms** | 2.79× |
| + HF_HUB_OFFLINE | `e4ce280` | 172ms | 254ms | **426ms** | 2.83× |
| + CRF CPU decode | `786e8e3` | 151ms | 259ms | **410ms** | 2.94× |
| 현재 HEAD | `HEAD` | 139ms | 258ms | **397ms** | 3.04× |

## 참고: 모델 로딩 시간(콜드스타트, 측정 대상 아님)

| 단계 | 로딩 |
|---|---:|
| (참고) 3-seed 앙상블 | 15.63s |
| 베이스라인2 단일 seed | 13.01s |
| + 워밍업 호출 | 13.23s |
| + Solar 호출 캐싱 | 14.81s |
| + gazetteer Aho-Corasick | 12.93s |
| + 청크 동시 실행 | 13.15s |
| + HF_HUB_OFFLINE | 11.55s |
| + CRF CPU decode | 11.41s |
| 현재 HEAD | 11.02s |

## 문서별 상세 (warm)

| 단계 | 문서 | 청크 | PII건 | 인젝션건 | PII ms | 인젝션 ms | 합계 ms |
|---|---|---:|---:|---:|---:|---:|---:|
| (참고) 3-seed 앙상블 | 카드분쟁접수서 | 2 | 26 | 2 | 284 | 111 | 396 |
| (참고) 3-seed 앙상블 | 고객지원리뷰 | 2 | 33 | 1 | 346 | 122 | 468 |
| **베이스라인2** 단일 seed | 카드분쟁접수서 | 2 | 26 | 2 | 172 | 113 | 285 |
| **베이스라인2** 단일 seed | 고객지원리뷰 | 2 | 33 | 1 | 217 | 122 | 339 |
| + 워밍업 호출 | 카드분쟁접수서 | 2 | 26 | 2 | 175 | 110 | 285 |
| + 워밍업 호출 | 고객지원리뷰 | 2 | 33 | 1 | 229 | 126 | 355 |
| + Solar 호출 캐싱 | 카드분쟁접수서 | 2 | 26 | 2 | 171 | 111 | 283 |
| + Solar 호출 캐싱 | 고객지원리뷰 | 2 | 33 | 1 | 216 | 122 | 338 |
| + gazetteer Aho-Corasick | 카드분쟁접수서 | 2 | 26 | 2 | 70 | 109 | 179 |
| + gazetteer Aho-Corasick | 고객지원리뷰 | 2 | 33 | 1 | 76 | 123 | 200 |
| + 청크 동시 실행 | 카드분쟁접수서 | 2 | 26 | 2 | 68 | 109 | 178 |
| + 청크 동시 실행 | 고객지원리뷰 | 2 | 33 | 1 | 80 | 125 | 205 |
| + HF_HUB_OFFLINE | 카드분쟁접수서 | 2 | 26 | 2 | 68 | 110 | 178 |
| + HF_HUB_OFFLINE | 고객지원리뷰 | 2 | 33 | 1 | 77 | 122 | 198 |
| + CRF CPU decode | 카드분쟁접수서 | 2 | 26 | 2 | 49 | 113 | 162 |
| + CRF CPU decode | 고객지원리뷰 | 2 | 33 | 1 | 58 | 123 | 181 |
| 현재 HEAD | 카드분쟁접수서 | 2 | 26 | 2 | 51 | 110 | 161 |
| 현재 HEAD | 고객지원리뷰 | 2 | 33 | 1 | 60 | 126 | 186 |

---

## 읽는 법

**탐지 결과는 전 단계에서 동일했다.** 모든 단계에서 PII 26건/33건, 인젝션 2건/1건이
그대로 나왔다. 속도만 바뀌었고 탐지 품질은 건드리지 않았다는 뜻이다(3-seed→단일
seed 도 마찬가지 — 세 seed 가 애초에 같은 가중치였다는 기존 진단과 일치한다).

**API → 로컬이 가장 큰 도약이다.** 10.97초 → 624ms(17.6배). 이 차이는 거의 전부
PII 쪽에서 나온다: API 는 청크 하나에서 PII 를 전부 뽑는 데 5초 안팎이 걸리는 반면
로컬 encoder 는 수십 ms 다. 인젝션은 API 도 937ms 로 상대적으로 빨랐다(판정만
하면 되고 뽑아낼 게 적어서다).

**정상 상태에서 실제로 시간을 줄인 개선은 두 개뿐이다.**

- `gazetteer Aho-Corasick`(1.64배) — 단일 개선으로는 가장 크다. PII 388ms → 146ms.
- `CRF CPU decode`(1.10배) — PII 145ms → 107ms.

여기에 베이스라인2 자체가 된 `3-seed → 단일 seed`(1.38배)까지 셋이 사실상 전부다.

**나머지 개선은 이 표에서 0으로 보이지만 잘못된 게 아니다** — 이 표가 재는 축이
아닐 뿐이다:

| 개선 | warm 에서 0인 이유 | 실제로 기여하는 곳 |
|---|---|---|
| 워밍업 호출 | 반복 측정이라 첫 호출 비용이 이미 지불된 뒤 | **첫 요청 표**: 1,206ms → 687ms (1.75배) |
| Solar 호출 캐싱 | Solar 2단계를 아예 끄고 쟀으므로 | Solar 를 켠 실사용 경로(동일 청크 재검사 0.86s → 0s) |
| 청크 동시 실행 | 겹칠 대상이 Solar 네트워크 대기인데 그걸 껐고, GPU 추론 구간은 detector 내부 락으로 어차피 직렬화됨 | Solar 를 켠 다청크 문서 |
| HF_HUB_OFFLINE | 탐지가 아니라 로딩 단계 동작 | **로딩 표**: 13.15s → 11.55s |

즉 이 네 가지는 "탐지 시간"이 아니라 **첫 응답 지연·로딩 시간·Solar 왕복**을 줄이는
개선이었고, 축을 맞춰 보면 전부 실제 효과가 확인된다. 특히 워밍업은 첫 요청 기준
인젝션 탐지를 647ms → 254ms(2.55배)로 줄여, 사용자가 체감하는 "첫 검사가 유독 느림"
문제를 없앤 개선이다.

**합산하면:** 정상 상태 기준 베이스라인2 대비 1.82배, API 베이스라인 대비 32배.
첫 요청 기준 베이스라인2 대비 3.04배.

## 덤으로 확인된 것 — API 베이스라인의 탐지 품질

속도와 별개로, API 베이스라인은 **인젝션을 놓쳤다**. `demo_chatgpt_고객지원리뷰.docx`
에는 실제 인젝션 문구가 한 군데 심겨 있는데(1,371–1,620) Solar Pro 3 는 5회 측정에서
매번 0건을 반환했다. 카드분쟁접수서에서는 1건을 정상적으로 찾았다. PII 건수는
로컬과 같은 26건/33건이 나왔지만, 스팬 단위 일치까지 비교하지는 않았다 — 건수만
같다는 뜻이다.

## 재현 방법

```bash
# 단계별 로컬 벤치(워크트리를 각 커밋으로 checkout 하며 순회)
REPEATS=7 bash engine/scripts/bench/run_stages.sh

# API 베이스라인
python engine/scripts/bench/bench_api.py \
  --engine-dir engine \
  --prompts "…/파이프라인/server/pipeline/prompts.py" \
  --env-file "…/securedoc-gateway/.env" \
  --repeats 5 --docs "…/demo_pii_카드분쟁접수서.docx" "…/demo_chatgpt_고객지원리뷰.docx" \
  --out engine/scripts/bench/results_api.json

# 표 생성
python engine/scripts/bench/gen_table.py
```

측정 원본 JSON 은 `engine/scripts/bench/results/` 와 `results_api.json` 에 함께 두었다.
